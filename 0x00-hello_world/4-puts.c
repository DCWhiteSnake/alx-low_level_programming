#include <stdio.h>

int main(void);
/**
 * main - Print the same message to the console
 * Return: 0 if no error
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
