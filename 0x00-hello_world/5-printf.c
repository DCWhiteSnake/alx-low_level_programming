#include <stdio.h>
/**
 * main - Print the same message to the console
 * Return: 0 always without error
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
