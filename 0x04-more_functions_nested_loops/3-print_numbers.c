#include "main.h"

/**
 * print_numbers - prints numbers
 * Return: 0 since there little room for errors
 */
void print_numbers(void)
{
	int i;

	for (i = 48; i <= 57; i++)
		_putchar(i);
	_putchar(10);
}
