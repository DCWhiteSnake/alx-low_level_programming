#include "main.h"
/**
 * mul - calculates the integer product of a and b.
 * @a: first integer to be multiplied.
 * @b: second integer to be multplied.
 * Return: the product of  a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}
